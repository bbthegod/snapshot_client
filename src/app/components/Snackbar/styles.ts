import { makeStyles } from '@material-ui/core/styles';
const useStyles = makeStyles({
  root: {
    position: 'fixed',
    width: '100%',
    background: '#262626',
    zIndex: 99999,
    bottom: 0,
    padding: '12px 16px',
    fontSize: 14,
    color: '#FFFFFF',
  },
});
export default useStyles;
