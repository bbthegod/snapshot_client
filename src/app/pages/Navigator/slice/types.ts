/* --- STATE --- */
export interface NavigatorState {
  data: any;
  searchData: any;
  notifications: any;
  loading: boolean;
  success: boolean;
  postSuccess: boolean;
  failures: boolean;
  snackbar: boolean;
  message: boolean;
}
